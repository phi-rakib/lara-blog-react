import React from "react";

function HorizontialDivider({ children }) {
  return (
    <>
      <h4 className="ui horizontal divider">{children}</h4>
    </>
  );
}

export default HorizontialDivider;
